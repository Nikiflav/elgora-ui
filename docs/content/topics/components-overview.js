export default {
  "id": "components-overview",
  "title": "Component overview",
  "path": "/components",
  "group": "components",
  "order": 10,
  "description": "Learn what an ElgoraUI Component adds to a native HTMLElement.",
  "toc": true,
  "api": [],
  "keywords": [
    "component",
    "HTMLElement",
    "lifecycle",
    "composition",
    "observable"
  ],
  "html": "<h1>Component overview</h1>\n<p><code>Component</code> is ElgoraUI’s small, composable abstraction over a native\n<code>HTMLElement</code>. It keeps the browser DOM as the actual rendering target, while\nadding the structure and behavior needed to build larger web applications.</p>\n<h2>Component and HTMLElement</h2>\n<p>An <code>HTMLElement</code> gives you a browser node: attributes, properties, events, and\nchildren. A <code>Component</code> owns an <code>HTMLElement</code> through its <code>dom</code> property and\nadds a consistent component boundary around it.</p>\n<p>The most important additions are:</p>\n<ul>\n<li><strong>Composition</strong> — <code>children</code> and <code>append()</code> accept text, numbers, native\nnodes, other Components, and nested arrays.</li>\n<li><strong>Lifecycle</strong> — <code>comcreate</code>, <code>commount</code>, <code>comunmount</code>, and <code>comdispose</code> let a\ncomponent initialize, connect, disconnect, and release resources.</li>\n<li><strong>State</strong> — a component can keep local state and create observable values\nthrough <code>com.observable()</code>.</li>\n<li><strong>Reactive rendering</strong> — <code>renderOnChange()</code> reruns a small rendering action\nwhen an <code>ObservableValue</code> changes.</li>\n<li><strong>Bindings</strong> — properties, attributes, styles, classes, and UI styles can be\nconnected to observable values with <code>bindProperty()</code>, <code>bindAttribute()</code>,\n<code>bindStyle()</code>, <code>bindClass()</code>, and <code>bindUiStyle()</code>.</li>\n<li><strong>Safe mounting</strong> — <code>mount()</code>, <code>unmount()</code>, <code>dispose()</code>, and <code>lock()</code> provide\npredictable ownership of the DOM tree.</li>\n</ul>\n<p>The component’s <code>dom</code> remains a normal <code>HTMLElement</code>, so native browser APIs\ncan still be used when needed. Use the Component API for changes to the\ncomponent tree so lifecycle behavior stays consistent.</p>\n<h2>Component lifecycle</h2>\n<p>A component has three distinct lifecycle phases. The phase determines whether\nthe component is connected to the document and which resources it should own.</p>\n<h3>Create</h3>\n<p>The constructor creates the component’s <code>dom</code>, applies its options, appends its\ninitial children, and invokes <code>comcreate</code> once. Use this phase for component-\nowned DOM setup and state initialization. A component does not have to be\nmounted yet, so code in this phase should not assume that <code>dom.isConnected</code> is\ntrue.</p>\n<h3>Mount and unmount</h3>\n<p><code>mount(container)</code> connects the component to an <code>HTMLElement</code> or CSS selector.\nIt invokes lifecycle callbacks for the complete component subtree in\nparent-to-child order:</p>\n<ol>\n<li><code>commount</code> option callback;</li>\n<li>protected <code>onMount()</code> hook;</li>\n<li>the same sequence for each child component.</li>\n</ol>\n<p><code>unmount()</code> removes the component from its parent without destroying it. The\ncomponent remains in memory, keeps its state, and can be mounted again. Its\ncallbacks run in the reverse order, children first and parent last.</p>\n<p>The callbacks are not copied from a parent to its children. Every component\nreceives its own lifecycle callbacks when the component tree is traversed.</p>\n<p>Use <code>onMount()</code> and <code>onUnmount()</code> for resources that depend on an external\nmount context, such as a listener on <code>window</code>, <code>document</code>, or the host element.\nPair every subscription created in <code>onMount()</code> with its removal in\n<code>onUnmount()</code> so repeated mount/unmount cycles do not create duplicate\nlisteners. Subscriptions to the component’s own <code>dom</code> can be created during\ncreation and remain available while the component is detached.</p>\n<p>Always call <code>mount()</code> and <code>unmount()</code> instead of directly appending or removing\n<code>dom</code>; direct DOM operations bypass lifecycle callbacks for the component and\nits descendants.</p>\n<h3>Dispose</h3>\n<p><code>dispose()</code> is the permanent end of the component lifecycle. It unmounts the\ncomponent, disposes its child components, releases registered resources, and\ninvokes <code>comdispose</code>. A disposed component cannot be mounted again. Use\n<code>dispose()</code> for resources owned for the entire lifetime of the component, while\n<code>onUnmount()</code> is for resources that only belong to one mounted instance.</p>\n<h2>Creating a component</h2>\n<p>The following example models a small status panel in an application shell. It\nkeeps its own state, listens to the external <code>window</code> object only while it is\nmounted, and can be unmounted and mounted again without losing that state.</p>\n<div data-live-demo=\"component-create\"></div>The essential shape is:\n<pre><code class=\"language-js\">class StatusPanel extends Component {\n  constructor() {\n    super({ tag: &quot;section&quot; });\n    this.status = e(&quot;p&quot;, &quot;Waiting for mount&quot;);\n    this.append(this.status);\n  }\n\n  onMount() {\n    this.updateStatus();\n    window.addEventListener(&quot;resize&quot;, this.updateStatus);\n  }\n\n  onUnmount() {\n    window.removeEventListener(&quot;resize&quot;, this.updateStatus);\n  }\n\n  updateStatus = () =&gt; {\n    this.status.textContent = `Mounted at ${window.innerWidth}px wide`;\n  };\n}\n\nconst panel = new StatusPanel();\npanel.mount(document.body);\n</code></pre>\n<p>For one-off DOM nodes, use <code>e()</code> directly. When a piece of UI owns state,\nlifecycle, or reusable behavior, use <code>Component</code> or one of the component\nfactories such as <a href=\"?!=/api-reference/cbutton\"><code>cbutton()</code></a>.</p>\n<h2>Composition with <code>e()</code> and component factories</h2>\n<p><code>e()</code> creates native elements and accepts nested children. Component factories\nsuch as <code>cbutton()</code> return Components, so both forms can be composed in the\nsame tree:</p>\n<pre><code class=\"language-js\">return [\n  e(&quot;p&quot;, &quot;Choose an action:&quot;),\n  cbutton({ children: &quot;Continue&quot; })\n];\n</code></pre>\n<p>The complete API is available in the <a href=\"?!=/api-reference/Component\"><code>Component</code> API reference</a>,\nincluding <a href=\"?!=/api-reference/ComponentOptions\"><code>ComponentOptions</code></a>,\n<a href=\"?!=/api-reference/ComponentChild\"><code>ComponentChild</code></a>, <a href=\"?!=/api-reference/e\"><code>e()</code></a>,\nand the component factories.</p>\n<h2>UI Styles</h2>\n<p>Components can use the ElgoraUI <code>ui</code> property to apply semantic styles and\nutility classes. UI Styles are documented separately, including the\n<a href=\"?!=/styles/semantic-colors\">semantic color system</a> and\n<a href=\"?!=/styles/html-elements\">HTML elements and utilities</a>.</p>\n",
  "demos": [
    {
      "id": "component-create",
      "source": "class StatusPanel extends Component {\n    constructor() {\n        super({\n            tag: \"section\",\n            ui: [\"elg\", \"surface\", \"box\", \"p-3\", \"d-flex\", \"flex-col\", \"gap-2\"]\n        });\n        this.updateStatus = () => {\n            this.status.textContent = `Mounted at ${window.innerWidth}px wide`;\n        };\n        const status = e(\"p\", { ui: [\"elg\", \"text-muted\", \"m-0\"] }, \"Waiting for mount\");\n        this.status = status;\n        this.append([\n            e(\"h3\", { ui: [\"elg\", \"m-0\"] }, \"A mounted Component has a lifecycle\"),\n            status\n        ]);\n    }\n    onMount() {\n        this.updateStatus();\n        window.addEventListener(\"resize\", this.updateStatus);\n    }\n    onUnmount() {\n        window.removeEventListener(\"resize\", this.updateStatus);\n    }\n}\nconst panel = new StatusPanel();\nconst toggle = e(\"button\", { ui: [\"elg\", \"btn\", \"neutral\"] }, \"Unmount panel\");\ntoggle.addEventListener(\"click\", () => {\n    if (panel.mounted) {\n        panel.unmount();\n        toggle.textContent = \"Mount panel\";\n    }\n    else {\n        panel.mount(demo);\n        toggle.textContent = \"Unmount panel\";\n    }\n});\nconst demo = e(\"div\", { ui: [\"elg\", \"d-flex\", \"flex-col\", \"gap-2\"] });\ndemo.append(panel.dom, toggle);\ndocument.body.append(demo);\npanel.mount(demo);",
      "code": "with (Elgora) {\nfunction createDemo() {\n    class StatusPanel extends Component {\n        constructor() {\n            super({\n                tag: \"section\",\n                ui: [\"elg\", \"surface\", \"box\", \"p-3\", \"d-flex\", \"flex-col\", \"gap-2\"]\n            });\n            this.updateStatus = () => {\n                this.status.textContent = `Mounted at ${window.innerWidth}px wide`;\n            };\n            const status = e(\"p\", { ui: [\"elg\", \"text-muted\", \"m-0\"] }, \"Waiting for mount\");\n            this.status = status;\n            this.append([\n                e(\"h3\", { ui: [\"elg\", \"m-0\"] }, \"A mounted Component has a lifecycle\"),\n                status\n            ]);\n        }\n        onMount() {\n            this.updateStatus();\n            window.addEventListener(\"resize\", this.updateStatus);\n        }\n        onUnmount() {\n            window.removeEventListener(\"resize\", this.updateStatus);\n        }\n    }\n    const panel = new StatusPanel();\n    const toggle = e(\"button\", { ui: [\"elg\", \"btn\", \"neutral\"] }, \"Unmount panel\");\n    toggle.addEventListener(\"click\", () => {\n        if (panel.mounted) {\n            panel.unmount();\n            toggle.textContent = \"Mount panel\";\n        }\n        else {\n            panel.mount(demo);\n            toggle.textContent = \"Unmount panel\";\n        }\n    });\n    const demo = e(\"div\", { ui: [\"elg\", \"d-flex\", \"flex-col\", \"gap-2\"] });\n    demo.append(panel.dom, toggle);\n    document.body.append(demo);\n    panel.mount(demo);\n}\ncreateDemo();\n}"
    }
  ]
};
