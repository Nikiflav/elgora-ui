export default {
  "id": "download",
  "title": "Download and installation",
  "path": "/getting-started/download",
  "group": "getting-started",
  "order": 2,
  "description": "Choose how to include ElgoraUI in a browser application.",
  "prev": "/getting-started/framework-overview",
  "next": "/components",
  "toc": true,
  "api": [],
  "keywords": [
    "download",
    "installation",
    "npm",
    "github",
    "static",
    "cdn"
  ],
  "html": "<h1>Download and installation</h1>\n<p>ElgoraUI can be used as a set of static browser assets or, in the future, through a package manager. The right option depends on whether the application is a plain HTML project, a build-based project, or a project that wants to track published releases.</p>\n<h2>Choose a distribution method</h2>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>What you need</th>\n<th>Best for</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Static bundles</td>\n<td><code>elgora-ui.js</code> and <code>elgora-ui.css</code></td>\n<td>Plain HTML applications and any HTTP server</td>\n</tr>\n<tr>\n<td>npm package</td>\n<td>Node.js and a package manager</td>\n<td>Vite, Webpack, or other build-based applications</td>\n</tr>\n<tr>\n<td>GitHub release assets</td>\n<td>A downloaded release archive</td>\n<td>Pinning and serving a specific version</td>\n</tr>\n<tr>\n<td>CDN</td>\n<td>A public URL to published assets</td>\n<td>Prototypes and simple demos</td>\n</tr>\n</tbody>\n</table>\n<p>The static bundle is the currently available distribution path. The npm, GitHub, and CDN links below are placeholders until the corresponding release workflow is published.</p>\n<h2>Static bundles from an HTTP server</h2>\n<p>Build or download the production files and copy them into the application’s public assets directory. The library build produces:</p>\n<pre><code class=\"language-text\">dist/\n├── elgora-ui.css\n├── elgora-ui.js\n└── elgora-ui.umd.cjs\n</code></pre>\n<p>For a plain browser application, include the CSS and the ESM JavaScript module in <code>index.html</code>:</p>\n<pre><code class=\"language-html\">&lt;!doctype html&gt;\n&lt;html lang=&quot;en&quot;&gt;\n&lt;head&gt;\n    &lt;meta charset=&quot;utf-8&quot;&gt;\n    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;\n    &lt;link rel=&quot;stylesheet&quot; href=&quot;./assets/elgora-ui.css&quot;&gt;\n&lt;/head&gt;\n&lt;body&gt;\n    &lt;main id=&quot;app&quot;&gt;&lt;/main&gt;\n    &lt;script type=&quot;module&quot; src=&quot;./assets/app.js&quot;&gt;&lt;/script&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre>\n<p>Then import ElgoraUI from the copied bundle in the application module:</p>\n<pre><code class=\"language-js\">import { cbutton } from &quot;./assets/elgora-ui.js&quot;;\n\ncbutton({\n    ui: [&quot;elg&quot;, &quot;btn&quot;, &quot;primary&quot;],\n    children: &quot;Hello from ElgoraUI&quot;\n}).mount(document.getElementById(&quot;app&quot;));\n</code></pre>\n<p>The static files must be served over HTTP. Use <code>npm run docs:build</code> to build the documentation site or <code>npm run build</code> to build the library distribution. Opening the HTML file directly with <code>file://</code> is not the supported deployment model because browser module and asset loading rules vary by browser.</p>\n<h2>npm package</h2>\n<p>The planned npm distribution will be the recommended option for applications that already use a JavaScript build tool. The expected workflow will look like this:</p>\n<pre><code class=\"language-bash\">npm install elgora-ui\n</code></pre>\n<pre><code class=\"language-js\">import { cbutton } from &quot;elgora-ui&quot;;\nimport &quot;elgora-ui/dist/elgora-ui.css&quot;;\n\ncbutton({\n    ui: [&quot;elg&quot;, &quot;btn&quot;, &quot;primary&quot;],\n    children: &quot;Installed from npm&quot;\n}).mount(document.getElementById(&quot;app&quot;));\n</code></pre>\n<p>Package link: <a href=\"https://www.npmjs.com/package/elgora-ui\">npm package — placeholder</a>.</p>\n<p>The package should expose ESM, UMD, TypeScript declarations, and the production CSS bundle. The CSS remains a separate import so applications can decide how and when to load the visual system.</p>\n<h2>GitHub release assets</h2>\n<p>For teams that want to download and archive a specific version, each release can later contain a versioned archive with the same static files:</p>\n<pre><code class=\"language-text\">elgora-ui-1.0.0/\n├── elgora-ui.css\n├── elgora-ui.js\n├── elgora-ui.umd.cjs\n└── index.d.ts\n</code></pre>\n<p>This is useful when the assets are served from an internal web server or committed into an application repository. Release link: <a href=\"https://github.com/your-org/elgora-ui/releases\">GitHub releases — placeholder</a>.</p>\n<h2>CDN</h2>\n<p>A CDN build can be useful for prototypes and small standalone demos. Once a release CDN exists, the usage could look like this:</p>\n<pre><code class=\"language-html\">&lt;link rel=&quot;stylesheet&quot; href=&quot;https://cdn.example.com/elgora-ui/1.0.0/elgora-ui.css&quot;&gt;\n&lt;script type=&quot;module&quot;&gt;\n    import { cbutton } from &quot;https://cdn.example.com/elgora-ui/1.0.0/elgora-ui.js&quot;;\n\n    cbutton({\n        ui: [&quot;elg&quot;, &quot;btn&quot;, &quot;primary&quot;],\n        children: &quot;CDN example&quot;\n    }).mount(document.getElementById(&quot;app&quot;));\n&lt;/script&gt;\n</code></pre>\n<p>CDN link: <a href=\"https://cdn.example.com/elgora-ui\">CDN documentation — placeholder</a>.</p>\n<p>For production applications, pin an exact version and use integrity metadata when the hosting provider supports it. Avoid depending on an unversioned <code>latest</code> URL.</p>\n<h2>Which option should I choose?</h2>\n<ul>\n<li>Use static bundles when the application should run from an ordinary HTTP server without a build step.</li>\n<li>Use npm when the application already has a build pipeline and should import only the modules it needs.</li>\n<li>Use GitHub release assets when the team controls deployment and wants an auditable, versioned archive.</li>\n<li>Use a CDN for prototypes, examples, and temporary experiments.</li>\n</ul>\n<h2>See also</h2>\n<ul>\n<li><a href=\"?!=/getting-started/framework-overview\">ElgoraUI fundamentals</a></li>\n<li><a href=\"?!=/components\">Components</a></li>\n</ul>\n",
  "demos": []
};
